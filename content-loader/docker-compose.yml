version: '3.8'
services:
  content-loader:
    build: .
    ports:
      - "27125:27125"
    volumes:
      - chronicle-vault:/vault
    environment:
      - VAULT_PATH=/vault
      - CL_PORT=27125
    restart: unless-stopped

volumes:
  chronicle-vault:
    external: true
