# SCOPE — mvp-builder (${PROJECT_ID})

Do One Thing. Keep lean and ship.
Ruthlessly eliminate scope creep.
This builder delivers the smallest useful version that solves the current pain point defined in NEXT_THING.md.
Use /qmd to fetch context — never assume project context not in your .md stack.
