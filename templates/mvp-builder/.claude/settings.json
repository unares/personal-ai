{
  "permissions": {
    "defaultMode": "bypassPermissions",
    "allow": [
      "Bash(*)",
      "Edit(**)",
      "Write(**)",
      "Read(**)",
      "WebSearch",
      "WebFetch(*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": ".claude/hooks/post-edit-lint.sh"
          }
        ]
      }
    ]
  },
  "mcpServers": {
    "qmd": {
      "command": "node",
      "args": ["/app/qmd/server.js"],
      "env": {
        "DB_PATH": "/app/shared/chronicle.db",
        "VAULT_PATH": "/app/vault"
      }
    }
  },
  "spinnerTipsEnabled": true,
  "spinnerTipsOverride": [
    "Try /yolo to skip all permission prompts",
    "Try /nextthing add: to queue up your next task",
    "Try /parking add: to park something for later",
    "Run /compact to free context when running long",
    "Run /handoff to save state before ending a session",
    "Use query_context MCP tool to fetch context from parent AIOO",
    "Ask Claude to use subagents for parallel work"
  ]
}
